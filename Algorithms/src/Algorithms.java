public class Algorithms {
    public static void main(String[] args) {

    }

    /**
     * TODO: Given an array of int, return the smallest number
     *
     * @param array of integers
     * @return the smallest number in the array
     */
    public static int smallest(int[] array){
        // Write your code here
        return 0;
    }

    /**
     * TODO: Given an array of int, return the first index of the smallest number
     *
     * @param array of integers
     * @return the index of the smallest number in the array
     */
    public static int indexOfSmallest(int[] array) {
        // Write your code here
        return 0;
    }

    /**
     * TODO: Given an array of int, return the first index of the smallest number
     *       after a specified starting index.
     *
     * @param array of integers
     * @param startIndex only consider values from this index forwards
     * @return the index of the smallest number after startIndex
     */
    public static int indexOfSmallestFrom(int[] array, int startIndex) {
        // Write your code here
        return 0;
    }

    /**
     * TODO: Given an array, swap the elements at index1 and index2
     *
     * @param array of integers
     * @param index1 the first element to be swapped
     * @param index2 the second element to be swapped
     */
    public static void swap(int[] array, int index1, int index2) {
        // Write your code here
    }

    /**
     * TODO: Given an array, sort the elements using selection sort
     *       Find the smallest element in the list, then swap it to index 0
     *       Find the smallest element in the list starting from index 1, then swap it to index 1
     *       Find the smallest element in the list starting from index 2, then swap it to index 2
     *       ... etc. until the list is sorted
     *
     * @param array of integers
     */
    public static void selectionSort(int[] array) {
        // Write your code here
    }

    /**
     * TODO: Given an array, search for the index of a specific number
     *       by iterating over each element of the list with a for loop
     *
     * @param array of integers
     * @param searched the integer to search for
     * @return the index of searched, or -1 if it isn't in the list
     */
    public static int linearSearch(int[] array, int searched) {
        // Write your code here
        return -1;
    }

    /**
     * TODO: Given an array, search for the index of a specific number
     *       using the binary search algorithm
     *
     * @param sortedArray of integers, assumed to be sorted
     * @param searched the integer to search for
     * @return the index of searched, or -1 if it isn't in the list
     */
    public static int binarySearch(int[] sortedArray, int searched) {
        // Write your code here
        return -1;
    }
}
